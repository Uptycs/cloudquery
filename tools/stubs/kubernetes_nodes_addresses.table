table_name("kubernetes_nodes_addresses")
description("Key value pairs describing addresees of kubernentes nodes.")
schema([
     Column("uid", TEXT, "Unique identiier of the node"),
     Column("hostname", TEXT, "hostname of the node "),
     Column("name", TEXT, "Name of the node"),
     Column("address_name", TEXT, "Name identifying address type"),
     Column("address_value", TEXT, "value of the adderess corresponding to name above"),
])
implementation("apiTable@genApiTable")
